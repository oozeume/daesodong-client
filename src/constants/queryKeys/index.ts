const QueryKeys = {
  facility: {
    visitedPets: 'facility visited pets',
    visitedUser: 'facility visited user',
    info: 'facility Info',
  },
  user: {
    getUser: 'get user',
  },
  auth: {},
} as const;

export default QueryKeys;
